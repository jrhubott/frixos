# Copy this file to .env and fill in your values.
# The .env file is gitignored and will not be published.

# OpenWeatherMap API key (get one at https://openweathermap.org/api)
WEATHER_API_KEY=your_openweathermap_api_key_here

# Manufacturer fallback network (optional). If stored WiFi credentials fail, the device
# will try this network before starting the provisioning portal. Leave empty to skip.
ARTLOGIC_SSID=
ARTLOGIC_PASSWORD=

# Location defaults (pre-populate NVS on first boot after flash; web UI can override)
LATITUDE=
LONGITUDE=
TIMEZONE=

# Home Assistant defaults (pre-populate NVS on first boot after flash; web UI can override)
HA_URL=
HA_TOKEN=
